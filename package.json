{
  "name": "rag-system",
  "version": "2.0.0",
  "private": true,
  "description": "Enterprise Strict Document-Only RAG Assistant with Full Multimodal Intelligence",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "build:all": "npm --prefix frontend install && npm --prefix frontend run build",
    "install:all": "npm --prefix frontend install",
    "dev": "npm --prefix frontend run dev"
  }
}
